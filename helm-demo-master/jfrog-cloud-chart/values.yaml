# Default values for testapi-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository:  docker-release-local.demo.jfrogchina.com/jfrog-cloud-demo
  tag: latest
  pullPolicy: IfNotPresent
service:
  name: jfrog-cloud-demo
  type: NodePort
  externalPort: 80
  nodePort: 30303
  internalPort: 8080
